# ------------- Application -------------
NODE_ENV=development
PORT=3000

# ------------- Database ----------------
# For local dev (Docker Compose 'db' service)
DATABASE_URL=postgresql://app:app@db:5432/walletsdb?schema=public

# ------------- Auth / JWT --------------
# Change in production!
JWT_SECRET=change-this-in-prod
# Durations can be "60m", "7d", "3600s", etc.
JWT_EXPIRES_IN=60m
REFRESH_TOKEN_EXPIRES_IN=7d

# ------------- Logging -----------------
# trace | debug | info | warn | error | fatal | silent
LOG_LEVEL=debug
# Pretty logs only in local dev (Pino transport)
PRETTY_LOGS=true

# ------------- CORS --------------------
# Comma-separated list of allowed origins in production.
# In dev/test, if empty, all origins are allowed.
# Example: https://your-frontend.app,https://admin.your-frontend.app
CORS_ORIGINS=http://localhost:3000

# ------------- Rate Limit --------------
# 15 minutes default window, 100 requests per window per IP
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100